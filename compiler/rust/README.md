# AIL Compiler Engine — Rust

**Status:** stub. High-performance backend targeting byte-identical canonical AIR
with the TypeScript reference. Conformance is defined by `../../tests/golden/`, not by
this implementation. See [`../README.md`](../README.md).
