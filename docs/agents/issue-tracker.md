# Issue Tracker

Tracker: The Question (skill-based, Claude Code)

The Question is a Claude Code skill that uses interrogative techniques to review
issues against the specification. Issues are created and tracked through skill
invocation, not an external service or CLI tool.

To create an issue: invoke `/the-question` with the issue description.
To review issues: invoke `/the-question` against the relevant spec section.
